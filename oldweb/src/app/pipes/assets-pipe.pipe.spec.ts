import { AssetsPipePipe } from './assets-pipe.pipe';

describe('AssetsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AssetsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
